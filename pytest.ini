[pytest]
testpaths = automation/tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    api: HTTP API checks against the local lab
    sql: database consistency and SQL checks
    ui: browser checks against the local lab
